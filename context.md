# Context

This project demonstrates an end-to-end fairness auditing workflow for AI decision systems. It combines a FastAPI backend, a Vite + React TypeScript frontend, and synthetic demo datasets that intentionally encode bias patterns for loan approval and hiring scenarios.
